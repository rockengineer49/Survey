"""
Merging logic: combine baseline, top-of-pipe, and downhole survey data according to rules.
"""
def merge_surveys(baseline, survey, downhole) -> 'MergedData':
    # Merge different survey inputs into unified form.
    pass

